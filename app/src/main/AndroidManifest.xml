<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.pizzeria_app">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.PizzeriaApp">
        <activity
            android:name=".PizzaActivity"
            android:label="Order Pizza"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".CustomizeActivity"
            android:label="Place Order"
            android:parentActivityName=".PizzaActivity">
        </activity>
        <activity
                android:name=".CurrentActivity"
                android:label="View Current Order"
                android:parentActivityName=".MainActivity">
        </activity>
        <activity
                android:name=".StoreActivity"
                android:label="All Store Orders"
                android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>